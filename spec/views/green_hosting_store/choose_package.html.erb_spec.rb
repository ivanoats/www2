require File.expand_path(File.dirname(__FILE__) + '/../../spec_helper')

describe "/green_hosting_store/choose_package" do
  before(:each) do
    # Consider using Cucomber to fully test Model -> View interaction
    # assigns[:cart] = Cart.new
    # assigns[:package] = Product.new
    # assigns[:packages] = [[ Product.new, Product.new ]]
    # render 'green_hosting_store/choose_package'
  end
end
