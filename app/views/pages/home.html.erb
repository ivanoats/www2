<h2><%= @page.title %></h2> 
<div class='text_content'>
  <%= @page.body %>
</div>
<% if logged_in_user and logged_in_user.has_role?('Administrator') %> 
<%= link_to "edit page", edit_page_path(@page) unless @page.new_record? %>
<% end %>